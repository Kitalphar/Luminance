<UserControl x:Class="Luminance.Controls.RecoveryKeyTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:Luminance.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="500">

    <UserControl.Resources>
        <sys:String x:Key="ClipboardSymbol">📋</sys:String>
        <sys:String x:Key="CheckmarkSymbol">✔</sys:String>
        <SolidColorBrush x:Key="SuccessColorBrush" Color="{DynamicResource SuccessColor}"/>
    </UserControl.Resources>


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Border
            Style="{DynamicResource RecoveryKeyTextBoxBorderStyle}"
            Grid.ColumnSpan="2"
            Panel.ZIndex="-1"/>

        <!-- The actual textbox -->
        <TextBox x:Name="KeyTextBox"
            Style="{DynamicResource RecoveryKeyTextBoxStyle }"
            Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

        <!-- Copy button -->
        <Button x:Name="CopyButton"
                Style="{DynamicResource RecoveryKeyTextBoxCopyButtonStyle}"
                Content="{StaticResource ClipboardSymbol}"
                Grid.Column="1"
                Click="CopyButton_Click"/>
    </Grid>
</UserControl>
